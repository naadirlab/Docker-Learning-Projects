version: '3.11'

services:
  web:
    build: .
    ports:
    - "5002:5002" 
    depends_on:
      - redis

  redis:
    image: "redis:latest"
    ports:
    - "6379:6379"  
    volumes:
    - redis-data:/data

volumes: 
  redis-data: